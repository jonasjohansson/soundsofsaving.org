---
layout: base.njk
current: about-us
permalink: /about-us/
title: About Us
eleventyComputed:
  description: "{{ pages.about.meta_description }}"
ogType: website
ogImage: "/assets/img/site/group.jpg"
ogImageAlt: "Four young people laughing together in a school gym"
---
{#
  About Us — intro/mission, Why Now (with stats + pullquote), Our Approach,
  Founding Story, Team, Board of Directors, Advisory Board. All copy from
  content/pages/about.json (CMS-editable). Section order mirrors the live site.
#}
{% set a = pages.about %}

{% pageCss "about" %}

<div class="container about-page">
  <div class="about-lede">
    {% for para in a.intro.split("\n\n") %}
      {% if loop.first %}<h1 class="about-intro">{{ para }}</h1>{% else %}<p class="about-intro">{{ para }}</p>{% endif %}
    {% endfor %}
    <p class="about-intro">Follow along on <a href="{{ settings.socials.instagram }}" target="_blank" rel="noopener">Instagram</a> and <a href="{{ settings.socials.facebook }}" target="_blank" rel="noopener">Facebook</a>, where we share new work and post regularly.</p>
  </div>
  {% photoRow a.photos, true %}

  {% if a.approach %}
  <section class="approach" aria-label="{{ a.approach.heading }}">
    <h2>{{ a.approach.heading }}</h2>
    {% if a.approach.intro %}<p class="approach__intro">{{ a.approach.intro }}</p>{% endif %}
    <ul class="principles">
      {% for pr in a.approach.principles %}
      <li class="principle"><h3>{{ pr.title }}</h3><p>{{ pr.body }}</p></li>
      {% endfor %}
    </ul>
  </section>
  {% endif %}

  {% if a.why_now %}
  <section class="about-section" aria-label="{{ a.why_now.heading }}">
    <h2>{{ a.why_now.heading }}</h2>
    <p>{{ a.why_now.body }}</p>
    {% if a.why_now.stats.length %}
    <ul class="stat-list">
      {% for s in a.why_now.stats %}<li>{{ s }}</li>{% endfor %}
    </ul>
    {% endif %}
    {% if a.why_now.pullquote %}
    <figure class="pullquote">
      <blockquote>{{ a.why_now.pullquote }}</blockquote>
      {% if a.why_now.pullquote_attribution %}<cite>{{ a.why_now.pullquote_attribution }}</cite>{% endif %}
    </figure>
    {% endif %}
    {% if a.why_now.sources.length %}
    <div class="about-sources">
      {% for src in a.why_now.sources %}<p>{{ src }}</p>{% endfor %}
    </div>
    {% endif %}
  </section>
  {% endif %}

  {% if a.research %}
  <aside class="research" aria-label="{{ a.research.heading }}">
    <div class="research__copy">
      <h2>{{ a.research.heading }}</h2>
      <p>{{ a.research.body | safe }}</p>
    </div>
    {% if a.research.cta %}
    <a class="btn btn--stroke" href="{{ a.research.cta.href }}">{{ a.research.cta.label }}</a>
    {% endif %}
  </aside>
  {% endif %}

  {% if a.founding_story %}
  <section class="about-section" aria-label="{{ a.founding_story.heading }}">
    <h2>{{ a.founding_story.heading }}</h2>
    {% for para in a.founding_story.body.split("\n\n") %}<p>{{ para }}</p>{% endfor %}
  </section>
  {% endif %}

  {% if a.team %}
  <section class="about-section" aria-label="{{ a.team.heading }}">
    <h2>{{ a.team.heading }}</h2>
    <ul class="people-grid">
      {% for m in a.team.members %}
      <li class="person">
        {% if m.photo %}<img class="person__photo" src="{{ m.photo }}" alt="{{ m.photo_alt or m.name }}" width="240" height="240" loading="lazy" decoding="async" />{% endif %}
        <p class="person__role">{{ m.role }}</p>
        <h3>{{ m.name }}</h3>
        {% if m.email %}<p class="person__email"><a href="mailto:{{ m.email }}">{{ m.email }}</a></p>{% endif %}
        {% for para in m.bio.split("\n\n") %}<p>{{ para }}</p>{% endfor %}
      </li>
      {% endfor %}
    </ul>
  </section>
  {% endif %}

  {% if a.board_of_directors %}
  <section class="about-section" aria-label="{{ a.board_of_directors.heading }}">
    <h2>{{ a.board_of_directors.heading }}</h2>
    <ul class="people-grid">
      {% for m in a.board_of_directors.members %}
      <li class="person">
        <h3>{{ m.name }}</h3>
        <p>{{ m.bio }}</p>
      </li>
      {% endfor %}
    </ul>
  </section>
  {% endif %}

  {% if a.advisory_board %}
  <section class="about-section" aria-label="{{ a.advisory_board.heading }}">
    <h2>{{ a.advisory_board.heading }}</h2>
    {% if a.advisory_board.intro %}<p>{{ a.advisory_board.intro }}</p>{% endif %}
    <ul class="roster">
      {% for m in a.advisory_board.members %}
      <li class="roster__item">
        <span class="roster__name">{{ m.name }}</span>
        {% if m.bio %}<span class="roster__role">{{ m.bio }}</span>{% endif %}
      </li>
      {% endfor %}
    </ul>
  </section>
  {% endif %}

  {% if pages.community.partners.logos %}
  <section class="about-partners" aria-label="Partners and supporters">
    <h2>Partners and supporters</h2>
    <ul class="logo-grid">
      {% for logo in pages.community.partners.logos %}
      <li>
        {% set mark %}<img src="{{ logo.src }}" alt="{{ logo.alt }}"{% if logo.w %} width="{{ logo.w }}" height="{{ logo.h }}"{% endif %} loading="lazy" decoding="async" />{% endset %}
        {% if logo.url %}<a href="{{ logo.url }}" target="_blank" rel="noopener">{{ mark | safe }}</a>{% else %}{{ mark | safe }}{% endif %}
      </li>
      {% endfor %}
    </ul>
  </section>
  {% endif %}
</div>
