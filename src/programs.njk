---
layout: base.njk
current: programs
permalink: /programs/
title: Programs
description: "Music is already a mental health tool. Sounds of Saving helps young people use it intentionally, through artist storytelling, a high-school program, and mental-health resources at concerts, festivals, and campuses."
ogType: website
---
{#
  Programs — program cards grouped under the Artists / Schools / Community
  taxonomy from the 2026 one-sheet. Each card follows a fixed schema: what it is / who it's for /
  what happens / proof / one CTA. Content from content/pages/programs.json
  (CMS-editable). The 988-co-branded Sessions series leads, marked featured.
#}
{% set p = pages.programs %}

<style>
  .programs-page { padding-block: var(--space-xl); }
  .programs-page h1 {
    font-size: var(--fs-title);
    line-height: var(--lh-tight);
    margin: 0 0 var(--space-md);
  }
  .programs-ctas { display: flex; flex-wrap: wrap; gap: var(--space-sm); margin: calc(-1 * var(--space-md)) 0 var(--space-xl); }
  .programs-lead {
    font-size: var(--fs-lead);
    line-height: var(--lh-base);
    max-width: var(--maxw-prose);
    margin: 0 0 var(--space-xl);
  }

  /* how-it-works loop */
  .howto { margin: 0 0 var(--space-2xl); }
  .howto__steps {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-md);
    margin-top: var(--space-md);
    padding: 0;
    list-style: none;
  }
  .howto__step {
    padding: var(--space-md);
  }
  .howto__label {
    color: var(--accent);
    margin: 0 0 var(--space-2xs);
  }
  .howto__step p { font-size: var(--fs-small); line-height: var(--lh-base); margin: 0; }

  /* group sections */
  .pgroup { margin-top: var(--space-2xl); }
  .pgroup__head { max-width: var(--maxw-prose); margin-bottom: var(--space-lg); }
  .pgroup__top { display: grid; grid-template-columns: minmax(0, 1fr) 320px; gap: var(--space-xl); align-items: end; margin-bottom: var(--space-lg); }
  .pgroup__top .pgroup__head { margin-bottom: 0; }
  .pgroup__photo { margin: 0; }
  .pgroup__photo .photo { display: block; aspect-ratio: 4 / 5; overflow: hidden; border-radius: var(--radius); background: var(--bg-alt); }
  .pgroup__photo img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .pgroup__photo figcaption { margin-top: var(--space-2xs); font-size: var(--fs-tiny); color: var(--text-faint); font-family: var(--font-mono); }
  @media (max-width: 760px) { .pgroup__top { grid-template-columns: 1fr; } }
  .pgroup__label {
    color: var(--accent);
    margin: 0 0 var(--space-2xs);
  }
  .pgroup__heading { font-size: var(--fs-h2); line-height: var(--lh-tight); margin: 0 0 var(--space-xs); }
  .pgroup__intro { line-height: var(--lh-base); margin: 0; }

  /* card grid */
  .pcards {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: var(--space-lg);
    list-style: none;
    padding: 0;
  }
  .pcard {
    display: flex;
    flex-direction: column;
    padding: var(--space-lg);
  }
  .pcard--featured {
    grid-column: 1 / -1;
    background: var(--dusk);
    border-color: var(--dusk-2);
    color: var(--on-dark);
  }
  .pcard--featured .pcard__title,
  .pcard--featured .pcard__dt { color: var(--on-dark); }
  .pcard--featured .pcard__dd { color: var(--paper-2); }
  .pcard--featured .pcard__eyebrow { color: var(--accent-bright); }

  .pcard__eyebrow {
    color: var(--accent);
    margin: 0 0 var(--space-2xs);
  }
  .pcard__title { font-size: var(--fs-h3); line-height: var(--lh-tight); margin: 0 0 var(--space-sm); }

  .pcard__spec { margin: 0 0 var(--space-md); }
  .pcard__dt {
    color: var(--ink-faint);
    margin-top: var(--space-sm);
  }
  .pcard__dt:first-child { margin-top: 0; }
  .pcard__dd { margin: var(--space-3xs) 0 0; font-size: var(--fs-small); line-height: var(--lh-base); max-width: none; }

  .pcard__cta--top { margin: 0 0 var(--space-md); }
  .pcard__dd + .pcard__dd { margin-top: var(--space-xs); }
  .pcard__proof { color: var(--text-muted); }

  /* educators pointer band */
  .edu-pointer {
    margin-top: var(--space-2xl);
    border: 1px solid var(--accent);
    border-radius: var(--radius);
    padding: var(--space-lg);
    background: var(--paper-3);
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-md);
  }
  .edu-pointer__copy { max-width: var(--maxw-prose); }
  .edu-pointer__eyebrow {
    color: var(--accent);
    margin: 0 0 var(--space-2xs);
  }
  .edu-pointer h2 { font-size: var(--fs-h3); margin: 0 0 var(--space-2xs); }
  .edu-pointer p { margin: 0; line-height: var(--lh-base); font-size: var(--fs-small); }

  @media (max-width: 720px) {
    .howto__steps { grid-template-columns: 1fr; }
  }
</style>

<div class="container programs-page">
  <h1>{{ p.heading or "Programs" }}</h1>
  {% if p.lead %}<p class="programs-lead">{{ p.lead }}</p>{% endif %}

  {% if p.ctas %}
  <p class="programs-ctas">{% for c in p.ctas %}<a class="btn {% if loop.first %}btn--primary{% else %}btn--ghost{% endif %}" href="{{ c.href }}">{{ c.label }}</a>{% endfor %}</p>
  {% endif %}

  {% for group in p.groups %}
  <section class="pgroup" id="{{ group.id }}" aria-label="{{ group.label }}">
    <div class="pgroup__top">
      <div class="pgroup__head">
        <h2 class="pgroup__heading">{{ group.heading }}</h2>
        {% if group.intro %}<p class="pgroup__intro">{{ group.intro }}</p>{% endif %}
      </div>
      {% if group.photo %}
      <figure class="pgroup__photo"><span class="photo"><img src="{{ group.photo.src }}" alt="{{ group.photo.alt }}" loading="lazy" decoding="async" /></span>{% if group.photo.caption %}<figcaption>{{ group.photo.caption }}</figcaption>{% endif %}</figure>
      {% endif %}
    </div>

    <ul class="pcards">
      {% for card in group.cards %}
      <li class="pcard{% if card.featured %} pcard--featured{% endif %}">
        <h3 class="pcard__title">{{ card.title }}</h3>
        {% if card.cta %}<p class="pcard__cta pcard__cta--top"><a class="btn {% if card.featured %}btn--stroke{% else %}btn--ghost{% endif %}" href="{{ card.cta.href }}">{{ card.cta.label }}</a></p>{% endif %}
        <div class="pcard__spec">
          <p class="pcard__dd">{{ card.body }}</p>
          {% if card.for %}<p class="pcard__dd"><strong>For:</strong> {{ card.for }}</p>{% endif %}
          {% if card.proof %}<p class="pcard__dd pcard__proof">{{ card.proof }}</p>{% endif %}
        </div>
      </li>
      {% endfor %}
    </ul>
  </section>
  {% endfor %}

  {% if p.educators_pointer %}
  <aside class="edu-pointer" aria-label="For educators">
    <div class="edu-pointer__copy">
      <h2>{{ p.educators_pointer.title }}</h2>
      <p>{{ p.educators_pointer.body }}</p>
    </div>
    {% if p.educators_pointer.cta %}
    <a class="btn btn--primary" href="{{ p.educators_pointer.cta.href }}">{{ p.educators_pointer.cta.label }}</a>
    {% endif %}
  </aside>
  {% endif %}
</div>
